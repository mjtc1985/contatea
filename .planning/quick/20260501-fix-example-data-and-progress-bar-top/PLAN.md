# Quick Task: Fix Example Data & Move Progress Bar

## Objective
1. Correct the mismatched example pairs (Image/Word) in the association game.
2. Finalize moving the progress bar to the top in `AssociationScreen`.

## Plan
1. Edit `lib/models/level.dart` to fix the default `AssociationPair` list.
2. Ensure `lib/screens/association_screen.dart` has `_buildProgress()` at the top.
3. Build and install Release APK.
4. Update `STATE.md`.
