# Quick Task: Move Progress Bar to Top

## Objective
Move the progress bar in `AssociationScreen` from the bottom to the top to match the layout of `CountingScreen`.

## Plan
1.  Modify `lib/screens/association_screen.dart` to move `_buildProgress()` call to the top of the main `Column`.
2.  Adjust padding/margins if necessary for visual consistency.
3.  Build and install on tablet.
4.  Update `STATE.md`.
